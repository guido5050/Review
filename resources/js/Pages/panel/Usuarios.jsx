import React from "react";

const Usuarios = ({ users }) => {
    console.log(users);
    return (
        <>
            <div>
                <h1>Usuarios</h1>
            </div>
        </>
    );
};

export default Usuarios;
